generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

model categorias {
    idcateg    Int     @id @default(autoincrement())
    nomecat String  @unique
    
}
model projeto {
    idprojeto    Int     @id @default(autoincrement())
    nome  String
    notas String
    estado String
    datainicio String
    datafim String
    idcat String
   //   categorias   categorias @relation(fields: [idcat], references: [idcateg])
}
model utilizadores{
    idutil String  @id @default(uuid())
    name String  @db.VarChar(255)
    email String @unique
    pass String
    isAdmin   Boolean @default(false)
}